# Data files (large Kaggle dataset — never commit) — but DO keep the nightly pulls
data/*
!data/daily/
*.csv
*.parquet

# Archives
*.zip

# Python
__pycache__/

# Temp / scratch scripts and tooling
_*.py
node_modules/
*.png
